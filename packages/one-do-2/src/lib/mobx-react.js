import {Provider as MRProvider} from 'mobx-react'

export const Provider = MRProvider
export {
  observer,
  PropTypes,
  inject,
  onError,
  componentByNodeRegistry,
  renderReporter,
  trackComponents,
  useStaticRendering,
} from 'mobx-react'
